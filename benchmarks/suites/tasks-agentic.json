{
  "repo": {
    "git_tag": "2.6.5",
    "local_path": ".sandbox/pytorch-lightning",
    "name": "pytorch-lightning",
    "namespace": [
      "lightning",
      "examples"
    ],
    "url": "https://github.com/Lightning-AI/pytorch-lightning"
  },
  "tasks": [
    {
      "difficulty": "simple",
      "id": "BA-01",
      "answer_contract": {"fields": ["production_importers", "rdep_counts", "ranking"], "params": {"ranking": {"candidate_set": "production_importers", "top_k": 1}}},
      "primary_module": "lightning.pytorch.callbacks.timer",
      "prompt": "The `Timer` callback in `lightning.pytorch.callbacks.timer` has a bug: when `duration` is expressed as a `timedelta`, it is compared against wall-clock seconds as if it were already a float, causing premature training stops on long runs.\n\nBefore touching the module, map the blast radius:\n1. Which modules directly import `lightning.pytorch.callbacks.timer`? List every one by full dotted name.\n2. For each direct importer, state how many other modules import it in turn \u2014 this is the second-order exposure.\n3. Identify the single importer with the highest second-order exposure; it carries the most amplified risk.\n\nProduce the direct-importer list with second-order counts and name the top-risk importer. Do not implement the fix.",
      "rdep_count": 3,
      "skill": "fix",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "medium",
      "id": "BA-02",
      "answer_contract": {"fields": ["production_importers", "rdep_counts", "ranking"], "params": {"ranking": {"candidate_set": "production_importers", "top_k": 2}}},
      "primary_module": "lightning.pytorch.core.optimizer",
      "prompt": "A regression in `lightning.pytorch.core.optimizer` causes the learning-rate scheduler step to fire twice per batch when `optimizer_step` is overridden in a `LightningModule`. The fix changes the internal step-counting logic.\n\nBefore touching the module, map the blast radius:\n1. Which modules directly import `lightning.pytorch.core.optimizer`? Enumerate all of them by full dotted name.\n2. For each direct importer, how many modules import it in turn? (second-order exposure)\n3. Of the direct importers, name the two with the highest second-order counts \u2014 those are the highest-risk files to regression-test.\n\nProduce the full direct-importer list, second-order counts, and the top-2 risk ranking. Do not implement the fix.",
      "rdep_count": 9,
      "skill": "fix",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "hard",
      "id": "BA-03",
      "answer_contract": {"fields": ["production_importers", "buckets", "ranking"], "params": {"buckets": {"labels": ["trainer-core", "callbacks", "everything-else"]}, "ranking": {"candidate_set": "production_importers", "top_k": 1}}},
      "primary_module": "lightning.pytorch.utilities.model_helpers",
      "prompt": "A silent bug in `lightning.pytorch.utilities.model_helpers` causes `is_overridden` to return `True` for methods that are inherited but not overridden, masking callback dispatch errors in certain model hierarchies.\n\nBefore fixing this, understand the full blast radius:\n1. List every module that directly imports `lightning.pytorch.utilities.model_helpers` by full dotted name.\n2. Group the importers into three exhaustive buckets: trainer-core contains only names beginning with `lightning.pytorch.trainer.`; callbacks contains only names beginning with `lightning.pytorch.callbacks.`; everything else contains every remaining importer, including `lightning.pytorch.core.*`, `lightning.pytorch.loops.*`, and `examples.*`.\n3. Among all importers, which single module is itself imported by the most other modules? That module concentrates the highest cascading risk.\n\nProduce the full importer list, the three-bucket grouping, and the highest-centrality importer. Do not implement the fix.",
      "rdep_count": 27,
      "skill": "fix",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "extreme",
      "id": "BA-04",
      "answer_contract": {"fields": ["production_importers", "high_centrality", "affected_module_count"], "params": {"high_centrality": {"min_rdep_count": 10}, "affected_module_count": {"min_rdep_count": 10}}},
      "primary_module": "lightning.pytorch.utilities.exceptions",
      "prompt": "A breaking change to the exception hierarchy in `lightning.pytorch.utilities.exceptions` \u2014 specifically renaming `MisconfigurationException` to `LightningConfigError` \u2014 is being evaluated for the next major release.\n\nBefore any rename, assess the true scope:\n1. List every non-test module that directly imports `lightning.pytorch.utilities.exceptions` by full dotted name \u2014 this is the direct production blast radius.\n2. Of those direct production importers, which are themselves imported by 10 or more non-test modules? These are the high-centrality intermediaries that amplify the blast radius into a second wave. Report each qualifying module with its exact non-test reverse-dependency count.\n3. Report the exact total number of unique modules affected if the rename propagates through the high-centrality intermediaries: the union of the direct production importers and every non-test module that directly imports a qualifying high-centrality intermediary. Deduplicate modules; do not sum overlapping rdep counts.\n\nProduce the direct importer list, the high-centrality module-to-rdep-count mapping, and the exact total affected module count. Do not implement any rename.",
      "rdep_count": 49,
      "skill": "fix",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "simple",
      "id": "BA-05",
      "answer_contract": {"fields": ["production_importers", "buckets", "ranking"], "params": {"buckets": {"labels": ["public", "internal"]}, "ranking": {"candidate_set": "production_importers", "top_k": 1}}},
      "primary_module": "lightning.pytorch.callbacks.finetuning",
      "prompt": "We want to add a `freeze_until_epoch` parameter to `lightning.pytorch.callbacks.finetuning` so users can defer layer unfreezing without subclassing.\n\nBefore implementing, scope the change:\n1. Which non-test modules import `lightning.pytorch.callbacks.finetuning`? List them all by full dotted name.\n2. Use this static benchmark boundary for each importer: classify `public` only when the importing module is a package initializer (`__init__.py`); classify every other importer as `internal`, including `examples.*`, regardless of documentation or re-export intent.\n3. Which importer would require the most careful update because it is itself most widely imported?\n\nProduce the importer list with public/internal classification and identify the highest-risk importer. Do not implement the feature.",
      "rdep_count": 2,
      "skill": "feature",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "medium",
      "id": "BA-06",
      "answer_contract": {"fields": ["production_importers", "ranking", "dependency_chain"], "params": {"ranking": {"candidate_set": "production_importers", "top_k": 2}, "dependency_chain": {"source": "lightning.pytorch.trainer.trainer", "target": "lightning.fabric.utilities.load"}}},
      "primary_module": "lightning.fabric.utilities.load",
      "prompt": "We are adding a `map_location` parameter to the load helpers in `lightning.fabric.utilities.load` so that checkpoints can be loaded directly to a target device without a separate `.to()` call.\n\nBefore writing any code, assess the integration surface:\n1. List every module that imports `lightning.fabric.utilities.load` by full dotted name \u2014 these are the callers that will gain access to the new parameter.\n2. Of those callers, which two have the most downstream importers of their own? Those two carry the highest amplification risk if their call sites change.\n3. Does `lightning.pytorch.trainer.trainer` depend on `lightning.fabric.utilities.load` directly or through one or more intermediaries? Trace the chain.\n\nProduce the caller list, the top-2 high-amplification callers, and the dependency chain (or 'not reachable'). Do not implement the feature.",
      "rdep_count": 8,
      "skill": "feature",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "benchmark_shaped": true,
      "difficulty": "extreme",
      "id": "BA-07",
      "answer_contract": {"fields": ["production_importers", "overlap_importers", "ranking"], "params": {"overlap_importers": {"targets": ["lightning.pytorch.utilities.rank_zero"]}, "ranking": {"candidate_set": "production_importers", "top_k": 3}}},
      "primary_module": "lightning.fabric.utilities.rank_zero",
      "prompt": "We want to add a `group` parameter to `lightning.fabric.utilities.rank_zero`'s logging utilities so that logging can be restricted to a specific process group instead of always defaulting to rank 0 of the world group.\n\nBefore writing any code, assess scope and coupling:\n1. Enumerate every module that imports `lightning.fabric.utilities.rank_zero` by full dotted name. This is your change surface.\n2. Among those importers, which modules also import `lightning.pytorch.utilities.rank_zero`? Modules on both lists call both rank-zero implementations and represent the highest consistency risk \u2014 the new parameter must be mirrored or a divergence will be introduced.\n3. Of the full importer list, rank the top 3 by their own importer count (highest rdep_count first).\n\nProduce the full importer list, the dual-importer overlap list, and the top-3 by centrality. Do not implement the feature.",
      "rdep_count": 28,
      "skill": "feature",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "extreme",
      "id": "BA-08",
      "answer_contract": {"fields": ["production_importers", "cross_namespace_importers", "ranking", "production_importer_count"], "params": {"cross_namespace_importers": {"prefix": "lightning.pytorch"}, "ranking": {"candidate_set": "production_importers", "top_k": 5}}},
      "primary_module": "lightning.fabric.utilities.types",
      "prompt": "We plan to add a new `ReduceOp` protocol type to `lightning.fabric.utilities.types` and deprecate the direct use of `torch.distributed.ReduceOp` throughout the codebase, replacing it with the new protocol.\n\nBefore touching anything, map the introduction surface:\n1. List all modules that import `lightning.fabric.utilities.types` by full dotted name. These modules will gain access to the new protocol type and are the primary propagation surface.\n2. Among those importers, which are part of the `lightning.pytorch` namespace? They indicate cross-namespace coupling and will require coordinated updates in both fabric and pytorch layers.\n3. Which five importers have the highest rdep counts of their own? Adding the new type to those modules amplifies exposure the most.\n4. Report the total importer count.\n\nProduce the full importer list, the cross-namespace list, the top-5 by centrality, and the total count. Do not implement the deprecation.",
      "rdep_count": 70,
      "skill": "feature",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "simple",
      "id": "BA-09",
      "answer_contract": {"fields": ["production_importers", "buckets", "ranking"], "params": {"buckets": {"labels": ["helper-dependent", "class-only"]}, "ranking": {"candidate_set": "helper_dependent_importers", "top_k": 100}}},
      "primary_module": "lightning.pytorch.callbacks.lr_finder",
      "prompt": "We are refactoring `lightning.pytorch.callbacks.lr_finder` to extract its internal `_lr_find` helper into a standalone function so it can be called without instantiating the callback.\n\nBefore making any changes:\n1. Which modules import `lightning.pytorch.callbacks.lr_finder`? List them by full dotted name \u2014 they are the only callers affected.\n2. For each importer, does it reference `_lr_find` directly (and therefore breaks if the helper is removed), or does it only instantiate the callback class? Classify each as `helper-dependent` or `class-only`.\n3. Rank the importers by risk: `helper-dependent` callers must be updated before the refactor can land.\n\nProduce the importer list with dependency classification and risk ranking. Do not implement the refactor.",
      "rdep_count": 3,
      "skill": "refactor",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "benchmark_shaped": true,
      "difficulty": "medium",
      "id": "BA-10",
      "answer_contract": {"fields": ["production_importers", "rdep_counts", "ranking"], "params": {"ranking": {"candidate_set": "production_importers", "top_k": 2}}},
      "primary_module": "lightning.fabric.plugins.environments.cluster_environment",
      "prompt": "We are refactoring `lightning.fabric.plugins.environments.cluster_environment` \u2014 specifically renaming the `creates_processes_externally` property to `is_interactive_compatible` to align with a new cross-framework naming convention.\n\nBefore touching the file:\n1. Enumerate every module that imports `lightning.fabric.plugins.environments.cluster_environment` by full dotted name. Each one must be audited for use of the old property name.\n2. For each importer, how many modules import it in turn? The importers with high second-order counts carry the most cascading rename risk.\n3. Name the top 2 importers by second-order count \u2014 those two should be prioritised in the rename sweep.\n\nProduce the full importer list with second-order counts, and identify the top-2 priority targets. Do not implement the rename.",
      "rdep_count": 13,
      "skill": "refactor",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "hard",
      "id": "BA-11",
      "answer_contract": {"fields": ["production_importers", "ranking", "cross_namespace_importers"], "params": {"ranking": {"candidate_set": "production_importers", "top_k": 5}, "cross_namespace_importers": {"prefix": "lightning.pytorch"}}},
      "primary_module": "lightning.fabric.utilities.distributed",
      "prompt": "We are refactoring `lightning.fabric.utilities.distributed` to replace its ad-hoc `barrier` wrappers with a unified `DistributedBarrier` context manager, which changes the call interface for every consumer.\n\nBefore touching the file:\n1. List every module that imports `lightning.fabric.utilities.distributed` by full dotted name. This is your mandatory update list.\n2. Of those importers, which five have the most downstream importers of their own? Changing those five modules carries the largest cascading risk and should be scheduled last in the refactor sequence.\n3. Does the refactor need to extend into the `lightning.pytorch` namespace? Identify any importers that are themselves in the `lightning.pytorch` package.\n\nProduce the full importer list, the top-5 by centrality in order, and the cross-namespace list. Do not implement the refactor.",
      "rdep_count": 23,
      "skill": "refactor",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "hard",
      "id": "BA-12",
      "answer_contract": {"fields": ["production_importers", "excluded_test_importer_count", "ranking", "production_importer_count"], "params": {"ranking": {"candidate_set": "production_importers", "top_k": 10}}},
      "primary_module": "lightning.pytorch.callbacks",
      "prompt": "We are refactoring `lightning.pytorch.callbacks` \u2014 the package `__init__` \u2014 to split its public exports into two sub-modules: `lightning.pytorch.callbacks.training` and `lightning.pytorch.callbacks.evaluation`, with the top-level `__init__` becoming a thin re-export shim.\n\nBefore restructuring anything:\n1. List every module that directly imports `lightning.pytorch.callbacks` by full dotted name. Include `import ... as ...`, `from lightning.pytorch import callbacks as ...`, and `from lightning.pytorch.callbacks import ...` forms; any such importer is affected by the export split.\n2. Use the shared test-module convention to report the excluded test-importer count.\n3. Among the remaining production importers, list the 10 highest rdep counts of their own, ordered by count descending then module name ascending. These are the highest-amplification paths and must be verified after the shim is in place.\n4. Report the total production importer count.\n\nProduce the production importer list, excluded test count, top-10 production centrality ranking, and total production count. Do not implement the refactor.",
      "rdep_count": 18,
      "skill": "refactor",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "simple",
      "id": "BA-13",
      "answer_contract": {"fields": ["production_importers", "rdep_counts", "isolation_verdict"], "params": {}},
      "primary_module": "lightning.pytorch.strategies.deepspeed",
      "prompt": "A PR modifies `lightning.pytorch.strategies.deepspeed` to add support for ZeRO stage 3 with CPU offloading, touching the `__init__` signature and internal setup hooks.\n\nReview the risk before approving:\n1. Which modules import `lightning.pytorch.strategies.deepspeed`? List them by full dotted name \u2014 each is a direct blast-radius target.\n2. For each direct importer, report its own rdep count. An importer with many downstream consumers amplifies the regression surface.\n3. Is this change isolated (all importers have rdep count \u2264 5) or does it reach widely-imported modules? State your conclusion.\n\nProduce the importer list with rdep counts and the isolation verdict. Do not approve or reject the PR.",
      "rdep_count": 3,
      "skill": "review",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "medium",
      "id": "BA-14",
      "answer_contract": {"fields": ["production_importers", "overlap_importers", "overlap_count", "ranking"], "params": {"overlap_importers": {"prefix": "lightning.fabric.plugins.precision."}, "overlap_count": {"prefix": "lightning.fabric.plugins.precision."}, "ranking": {"candidate_set": "production_importers", "top_k": 3}}},
      "primary_module": "lightning.fabric.plugins.precision.utils",
      "prompt": "A PR proposes changing the signature of the `_convert_fp_tensor` helper in `lightning.fabric.plugins.precision.utils`, replacing the `dtype` positional argument with a keyword-only argument to prevent silent argument-order bugs.\n\nQuantify the risk before approving:\n1. List every module that imports `lightning.fabric.plugins.precision.utils` by full dotted name. Each one is a potential call-site that must be updated.\n2. How many of those importers also import other modules from `lightning.fabric.plugins.precision.*`? Modules that couple to multiple precision utilities are the most fragile \u2014 list them.\n3. Of all direct importers, which three have the highest rdep counts? Those carry the most downstream regression risk.\n\nProduce the full importer list, the multi-precision-coupled list, and the top-3 by centrality. Do not approve or reject the PR.",
      "rdep_count": 11,
      "skill": "review",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "hard",
      "id": "BA-15",
      "answer_contract": {"fields": ["production_importers", "overlap_importers", "ranking"], "params": {"overlap_importers": {"targets": ["lightning.pytorch.utilities.rank_zero", "lightning.pytorch.utilities.enums"]}, "ranking": {"candidate_set": "non_migrated_importers", "exclude_overlap_targets": ["lightning.pytorch.utilities.rank_zero", "lightning.pytorch.utilities.enums"], "top_k": 100}}},
      "primary_module": "lightning.pytorch.utilities",
      "prompt": "A PR removes three deprecated symbols from `lightning.pytorch.utilities` (the package `__init__`): `rank_zero_deprecation`, `AMPType`, and `GradClipAlgorithmType`. The symbols have been in the deprecated state for two major versions.\n\nBefore approving, assess the true removal risk:\n1. List every module that imports `lightning.pytorch.utilities` by full dotted name. These are all candidates for using the removed symbols.\n2. Among those importers, which also import `lightning.pytorch.utilities.rank_zero` or `lightning.pytorch.utilities.enums` directly? Those modules may have already migrated to the canonical import path and are lower risk \u2014 flag them as `migrated-candidate`.\n3. Rank the remaining importers (not flagged as migrated) by their own rdep counts, highest first \u2014 these are the highest regression risk if any of the three symbols are still in use.\n\nProduce the full importer list, the migrated-candidate list, and the non-migrated importers ranked by centrality. Do not approve or reject the PR.",
      "rdep_count": 13,
      "skill": "review",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "extreme",
      "id": "BA-16",
      "answer_contract": {"fields": ["production_importers", "high_centrality", "test_importer_count", "production_importer_count", "risk_tier"], "params": {"high_centrality": {"min_rdep_count": 10}, "risk_tier": {"critical_min_production_importer_count": 10, "critical_min_high_centrality_count": 1}}},
      "primary_module": "lightning.pytorch.utilities.rank_zero",
      "prompt": "A PR replaces the `rank_zero_warn` function in `lightning.pytorch.utilities.rank_zero` with a new `rank_zero_warn_once` implementation that deduplicates repeated warnings, changing the function signature to add an optional `key` parameter.\n\nBefore approving, conduct a full risk assessment:\n1. List every production module that directly imports `lightning.pytorch.utilities.rank_zero` by full dotted name. This is the production direct-exposure surface.\n2. Among those production importers only, report each module with 10 or more non-test direct importers of its own. These are the production high-centrality intermediaries \u2014 any breakage in them cascades widely.\n3. Count direct importers below conventional test-root directories separately from the production importer count.\n4. Assign risk tier deterministically: `critical` requires at least 10 production importers and at least one reported high-centrality intermediary; otherwise use `high` when any high-centrality intermediary exists, `medium` when any production importer exists, or `low` when none exists.\n\nProduce the production importer list, high-centrality production mapping, test vs production counts, and the deterministic risk-tier verdict. Do not approve or reject the PR.",
      "rdep_count": 64,
      "skill": "review",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "extreme",
      "id": "BA-17",
      "answer_contract": {"fields": ["production_importer_count", "excluded_test_importer_count", "rdep_counts", "ranking"], "params": {"ranking": {"candidate_set": "production_importers", "top_k": 25}}},
      "primary_module": "lightning.pytorch",
      "prompt": "We are preparing a major-version change to the `lightning.pytorch` package `__init__`: the eager re-exports become lazy attribute lookups, so every module that imports the package by name changes its import-time behaviour.\n\nBefore committing to the change, size the whole affected surface:\n1. Report the total production importer count for `lightning.pytorch`.\n2. Use the shared test-module convention to report the excluded test-importer count.\n3. For every production importer, report its own rdep count. Report the complete mapping, one entry per production importer, with no truncation, no sampling, and no elision.\n4. List the 25 highest rdep counts among those production importers, ordered by count descending then module name ascending. These are the modules whose own dependents feel the lazy-import change first.\n\nProduce the total production count, the excluded test count, the complete per-importer rdep count mapping, and the top-25 centrality ranking. Do not implement the change.",
      "rdep_count": 94,
      "skill": "refactor",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "extreme",
      "id": "BA-18",
      "answer_contract": {"fields": ["production_importers", "rdep_counts", "high_centrality"], "params": {"high_centrality": {"min_rdep_count": 3}}},
      "primary_module": "lightning.fabric.utilities.imports",
      "prompt": "We are replacing the eager availability flags in `lightning.fabric.utilities.imports` with a lazy capability registry resolved on first access, which changes when import errors surface for every consumer.\n\nBefore the swap, establish the consumer surface:\n1. List every production module that directly imports `lightning.fabric.utilities.imports` by full dotted name.\n2. For every module in that list, report its own rdep count. Report the complete mapping, one entry per production importer, with no truncation, no sampling, and no elision.\n3. From that same mapping, report the subset whose rdep count is at least 3, with their counts. These are the consumers whose own dependents inherit the deferred failure mode.\n\nProduce the production importer list, the complete per-importer rdep count mapping, and the high-centrality subset with counts. Do not implement the registry.",
      "rdep_count": 33,
      "skill": "feature",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "extreme",
      "id": "BA-19",
      "answer_contract": {"fields": ["production_importers", "production_importer_count", "excluded_test_importer_count", "ranking"], "params": {"ranking": {"candidate_set": "production_importers", "top_k": 20, "min_rdep_count": 2}}},
      "primary_module": "lightning.fabric.plugins",
      "prompt": "We are splitting `lightning.fabric.plugins` into `lightning.fabric.plugins.io` and `lightning.fabric.plugins.compute`, leaving the current package as a re-export shim for one release.\n\nBefore the split, map who depends on the current package:\n1. List every production module that directly imports `lightning.fabric.plugins` by full dotted name.\n2. Report the total production importer count.\n3. Use the shared test-module convention to report the excluded test-importer count.\n4. Among the production importers, list the 20 highest rdep counts, counting only importers whose own rdep count is at least 2, ordered by count descending then module name ascending. These are the modules that must be migrated off the shim first.\n\nProduce the production importer list, the total production count, the excluded test count, and the top-20 ranking. Do not implement the split.",
      "rdep_count": 29,
      "skill": "refactor",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    },
    {
      "difficulty": "extreme",
      "id": "BA-20",
      "answer_contract": {"fields": ["production_importers", "rdep_counts", "overlap_importers", "overlap_count"], "params": {"overlap_importers": {"prefix": "lightning.pytorch"}, "overlap_count": {"prefix": "lightning.pytorch"}}},
      "primary_module": "lightning.fabric.strategies",
      "prompt": "We are moving strategy registration in `lightning.fabric.strategies` from import-time side effects to an explicit registry call, which requires every consumer to opt in.\n\nBefore the migration, map the consumer surface and its cross-layer coupling:\n1. List every production module that directly imports `lightning.fabric.strategies` by full dotted name.\n2. For every module in that list, report its own rdep count. Report the complete mapping, one entry per production importer, with no truncation, no sampling, and no elision.\n3. From that list, report the modules that also statically import anything under the `lightning.pytorch` namespace. These need coordinated changes in both layers.\n4. Report how many modules are in that cross-layer subset.\n\nProduce the production importer list, the complete per-importer rdep count mapping, the cross-layer subset, and its count. Do not implement the migration.",
      "rdep_count": 23,
      "skill": "refactor",
      "type": "blast_radius_analysis",
      "workflow_subtype": "pre_implementation_research",
      "workflow_type": "query"
    }
  ]
}
