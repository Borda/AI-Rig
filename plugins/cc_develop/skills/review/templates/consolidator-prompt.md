<!-- file: consolidator-prompt.md — consumers: review/SKILL.md §Step 5 -->

# Consolidator Agent Prompt — develop:review

Pre-expansion required: before building Agent spawn call, substitute these bare shell variables with resolved literal values:

- $REPORT_DIR_LITERAL — report directory path (resolved in Step 2)
- $DATE — from `date +%Y-%m-%d` output
- $REVIEW_CHECKLIST — resolved checklist path or empty string
- $REVIEW_TEMPLATE — resolved review-report.md template path (Step 5); consolidator reads it itself — never embed the template body in this prompt (double-bills its tokens)

`$RUN_DIR` is the exception — do NOT substitute it. Run-dir preamble (prepended by SKILL.md §Step 5) makes consolidator self-resolve `$RUN_DIR` via `cat "${TMPDIR:-/tmp}/dev-review-run-dir-${CSID}"`; leave `$RUN_DIR` literal in prompt text.

## Full Prompt

"Read all finding files in `$RUN_DIR/` (agent files: `sw-engineer.md`, `qa-specialist.md`, `perf-optimizer.md`, `doc-scribe.md`, `linting-expert.md`, `solution-architect.md`, `codex.md` if present — skip missing; merged spawns write one file per dimension, list unchanged). Read report template at `<REVIEW_TEMPLATE>` (Read tool) — defines output structure. Read `<REVIEW_CHECKLIST>` (Read tool), apply consolidation rules (signal-to-noise filter, annotation completeness, section caps). **`<REVIEW_CHECKLIST>` empty/unset:** insert top-level note: 'Review checklist not applied (oss plugin not available) — severity anchors may be inconsistent.' Check `$RUN_DIR/cross-validation.md` — contains 'Cross-Validation: SKIPPED' → prepend `Cross-validation: skipped` to YAML header and executive summary. Precision gate: only findings with concrete, actionable location (function, line range, or variable name). Finding density: modules under 100 lines → aim ≤10 total findings. Rank by impact (blocking > critical > high > medium > low). **File head — MANDATORY format:** file MUST begin with a `---`-delimited YAML metadata block exactly as in `review-report.md` template — opening `---` line 1, then the 12 fields in order (`develop-review:`, `Date:`, `Change Type:`, `Scope:`, `Focus:`, `Agents:`, `CI:`, `Outcome:`, `Summary:`, `Confidence:`, `Next steps:`, `Path:`), closing `---`, then body. No HTML comments (`<!-- ... -->`) or prose — orchestrator reads `---` block verbatim as reply header. `develop-review:` = target (file / dir / working-tree diff). `Confidence:` aggregate score — key gaps. `Path:` `→ <REPORT_DIR_LITERAL>/review-report.md`. **Header fields**: `Date:` <DATE>; `Change Type:` — classify from change INTENT, reading diff directly; never file-count heuristics or commit message prefix. Fix and feat can touch same files; type = what change DOES: `fix`=corrects wrong behavior, `feat`=adds capability/API, `refactor`=same behavior restructured, `perf`=same behavior faster, `docs`=docs/comments only, `ci`=pipeline only, `chore`=deps/tooling, `test`=tests only, `mixed`=multiple intents; `Scope:` comma-separated key changed py files (cap ~5); `Focus:` scope-label em-dash one-line description of what change does (from diff, not commit message); `Agents:` comma-separated agent names with output in `$RUN_DIR/`; `CI:` fixed value `N/A (develop:review is read-only — runs no tests)` — never fabricate pass/fail; `Outcome:` APPROVE | NEEDS_WORK | REQUEST_CHANGES; `Summary:` 1-2 sentence overview; `Next steps:` comma-separated blockers + high-priority actions (max 5). **Severity tiers:** every finding carries explicit inline label: `[cosmetic]`, `[low]`, `[medium]`, `[high]`, `[critical]`. Cosmetic findings → dedicated `### Cosmetic / Style` section. For `codex.md`: unique findings under `### Codex Co-Review`; same file:line → keep agent version, mark 'also flagged by Codex'. `codex` fixed confidence 0.75. Write consolidated report to `<REPORT_DIR_LITERAL>/review-report.md`. After `## Confidence`, append `## Source Files` via `Glob(pattern='*.md', path='$RUN_DIR')`. Return ONLY compact JSON: {"status":"done","findings":N,"severity":{"critical":N,"high":N,"medium":N,"low":N},"file":"\<REPORT_DIR_LITERAL>/review-report.md","confidence":0.N,"summary":"<one-line verdict>"}"
