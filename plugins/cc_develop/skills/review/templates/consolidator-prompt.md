<!-- file: consolidator-prompt.md — consumers: review/SKILL.md §Step 5 -->

**Reviewer attribution (additive):** Preserve the aggregate prose summary, existing metadata fields, overall verdict, confidence and detailed sections. Add `Reviewers:` immediately after `Agents:`, listing actual readable role names and their scoped rating, for example `Software engineer (3), QA specialist (2), Documentation reviewer (1).` Ratings: 1 Approve, 2 Minor changes, 3 Changes required, 4 Insufficient evidence, 5 Block / Reject. Use each reviewer's explicit rating and rationale; a reviewer that ran without stating one is 4, never inferred approval. A reviewer that produced no output is not rated — record it as a missing-reviewer limitation instead. Label parent substitutes explicitly; skipped roles are omitted. Never average ratings or replace the overall verdict. Retain all contributing roles when deduplicating. Add the template's findings overview with `ID | Author | Finding | Resolution proposal | Status`; Author lists all originating reviewer roles, not the consolidator or PR author. Preserve existing detailed sections and cross-reference stable IDs. Immediately after the closing metadata delimiter write exactly: `Legend: 1 = Approve · 2 = Minor changes · 3 = Changes required · 4 = Insufficient evidence · 5 = Block / Reject.` In terminal output the legend belongs immediately below the header table. Terminal gate rejection retains its existing behavior; use `Reviewers: Not assessed` when no reviewer assessed the source.

# Consolidator Agent Prompt — develop:review

Pre-expansion required: before building Agent spawn call, substitute these bare shell variables with resolved literal values:

- $REPORT_DIR_LITERAL — report directory path (resolved in Step 2)
- $DATE — from `date +%Y-%m-%d` output
- $REVIEW_CHECKLIST — resolved checklist path or empty string
- $REVIEW_TEMPLATE — resolved review-report.md template path (Step 5); consolidator reads it itself — never embed the template body in this prompt (double-bills its tokens)

`$RUN_DIR` is the exception — do NOT substitute it. Run-dir preamble (prepended by SKILL.md §Step 5) makes consolidator self-resolve `$RUN_DIR` via `cat "${TMPDIR:-/tmp}/dev-review-run-dir-${CSID}"`; leave `$RUN_DIR` literal in prompt text.

## Full Prompt

"Read all finding files in `$RUN_DIR/` (agent files: `sw-engineer.md`, `qa-specialist.md`, `perf-optimizer.md`, `doc-scribe.md`, `linting-expert.md`, `solution-architect.md`, `challenger.md` if present, `blind-solve.md` if present, `codex.md` if present — skip missing; merged spawns write one file per dimension, list unchanged). For `challenger.md`: map its findings by section before merging — Blockers (`[CRITICAL]`) → `critical` or `high` (promote to `critical` only when independently corroborated, e.g. a Step 4 CONFIRMED verdict or matching evidence from another agent; otherwise `high`), Concerns (`[HIGH]`) → `medium`, Nitpicks (`[LOW]`) → `low`. Never drop challenger findings. `blind-solve.md` present: compare its blueprint against the diff and the other agents' findings — add a `### Design Divergence` section listing where the independent blueprint differs from the PR's approach, ranked by impact; for each divergence mark whether it might be explained by context the blind-solve agent lacked (prior decision, incident, constraint) vs a genuine miss. Divergences already covered by another agent's finding: don't duplicate, cross-reference instead. No `blind-solve.md` (skipped or FIX/REFACTOR/CHORE): omit the section entirely, no placeholder note. Read report template at `<REVIEW_TEMPLATE>` (Read tool) — defines output structure. Read `<REVIEW_CHECKLIST>` (Read tool), apply consolidation rules (signal-to-noise filter, annotation completeness, section caps). **`<REVIEW_CHECKLIST>` empty/unset:** insert top-level note: 'Review checklist not applied (oss plugin not available) — severity anchors may be inconsistent.' Check `$RUN_DIR/cross-validation.md` — contains 'Cross-Validation: SKIPPED' → prepend `Cross-validation: skipped` to YAML header and executive summary. Precision gate: only findings with concrete, actionable location (function, line range, or variable name). Finding density: modules under 100 lines → aim ≤10 total findings. Rank by impact (blocking > critical > high > medium > low). **File head — MANDATORY format:** file MUST begin with a `---`-delimited YAML metadata block exactly as in `review-report.md` template — opening `---` line 1, then the 14 fields in order (`develop-review:`, `Title:`, `Date:`, `Change Type:`, `Scope:`, `Focus:`, `Agents:`, `Reviewers:`, `CI:`, `Outcome:`, `Summary:`, `Confidence:`, `Next steps:`, `Path:`), closing `---`, then body. No HTML comments (`<!-- ... -->`) or prose — orchestrator reads `---` block verbatim as reply header. `develop-review:` = target (file / dir / working-tree diff). `Title:` = `develop-review — <target>`; required by the report-delivery hook. `Confidence:` aggregate score — key gaps. `Path:` `→ <REPORT_DIR_LITERAL>/review-report.md`. **Header fields**: `Date:` <DATE>; `Change Type:` — classify from change INTENT, reading diff directly; never file-count heuristics or commit message prefix. Fix and feat can touch same files; type = what change DOES: `fix`=corrects wrong behavior, `feat`=adds capability/API, `refactor`=same behavior restructured, `perf`=same behavior faster, `docs`=docs/comments only, `ci`=pipeline only, `chore`=deps/tooling, `test`=tests only, `mixed`=multiple intents; `Scope:` comma-separated key changed py files (cap ~5); `Focus:` scope-label em-dash one-line description of what change does (from diff, not commit message); `Agents:` comma-separated agent names with output in `$RUN_DIR/`; `CI:` fixed value `N/A (develop:review is read-only — runs no tests)` — never fabricate pass/fail; `Outcome:` APPROVE | NEEDS_WORK | REQUEST_CHANGES; `Summary:` 1-2 sentence overview; `Next steps:` comma-separated blockers + high-priority actions (max 5). **Severity tiers:** every finding carries explicit inline label: `[cosmetic]`, `[low]`, `[medium]`, `[high]`, `[critical]`. Cosmetic findings → dedicated `### Cosmetic / Style` section. For `codex.md`: unique findings under `### Codex Co-Review`; same file:line → keep agent version, mark 'also flagged by Codex'. `codex` fixed confidence 0.75. Write consolidated report to `<REPORT_DIR_LITERAL>/review-report.md`. After `## Confidence`, append `## Source Files` via `Glob(pattern='*.md', path='$RUN_DIR')`. Return ONLY compact JSON: {"status":"done","findings":N,"severity":{"critical":N,"high":N,"medium":N,"low":N},"file":"\<REPORT_DIR_LITERAL>/review-report.md","confidence":0.N,"summary":"<one-line verdict>"}"
