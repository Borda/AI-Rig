# Final Handoff Contract

The post-gate presentation checkpoint makes the workflow's final user-facing structure executable. Artifact workflows must write `<run-directory>/final-handoff.json`, render it with `python PLUGIN_ROOT/shared/final_handoff.py render --handoff <run-directory>/final-handoff.json --out-final <run-directory>/final.md --out-validation <run-directory>/final-handoff.validation.json`, bind returned digests under `result.metadata.final_handoff`, pass all skill-specific and shared artifact validators, promote the candidate, then emit validated `final.md` bytes verbatim. Never manually reconstruct or summarize the response after validation.

Rendered section and table labels use portable Markdown bold text such as `**Outcome**` and `**Verification**`. Never emit ATX/Setext headings, ANSI color escapes, or renderer-specific HTML styling; final reports must stay compact and readable in monochrome terminals, saved Markdown, plain logs.

This is a post-gate checkpoint, not a sixth quality gate. Order is fixed: five gates; final-handoff creation and rendering; result candidate using schema 3 for every current Code Review disposition or schema 2 for other skills; skill validator; shared validator including `final_handoff.py check`; candidate promotion; verbatim final output. A failure in the checkpoint blocks promotion and completion.

Code-remediate has one explicit continuation before promotion and final output: prepare its commit disposition and any eligible plan, render and validate the candidate, then execute its step-12 commit decision. A pending native question keeps commit work pending; a blocked or inapplicable commit still needs an explicit disposition. Refresh the handoff and revalidate after an answer or execution changes that disposition, then promote and emit it. Result validation alone must never skip this continuation.

## Schema

New assessed code-review `result.candidate.json` promotion and current schema-3 `result.json` validation require a nonempty reviewer assessment list and exactly one matching attributed snapshot for its scope, including the bound aggregate summary. Schema-2 results and schema-3 results without retained proof remain readable as unverified archives and cannot certify completion or remediation intake.

This is a breaking requirement for newly built non-PR assessed results: they previously promoted with no snapshot table at all and must now carry a `Review Snapshot`. Rejection is `code-review-final-handoff-candidate-attribution-missing`. Current schema-3 candidate and promoted results are gated; historical schema-2 artifacts remain readable.

Wherever a `Review Snapshot` is present, assessed validation binds its rows exactly as it binds `PR Snapshot`: fields `Scope`, `Revision`, `CI`, `Type`, `Suggestion` in that order, and a `Suggestion` matching the canonical recommendation. For current schema-3 non-PR results, `Scope` equals normalized `metadata.scope` (`working-tree`, `path`, or `commit`) and `Revision` is exactly `diff sha256:<digest>` from retained `diff.patch`; validation rehashes the file against `metadata.review_input_sha256`. This identifies reviewed diff bytes, not a target path or commit identity; the collector has no structured coordinate for those values. Mismatches raise the corresponding snapshot field, value, suggestion, or retained-diff errors. The heading is new in this release, so no stored artifact predates the binding. Existing attribution shape, author binding, terminal exceptions and caller-contract rules remain unchanged.

New assessed code reviews add `reviewers` to `PR Snapshot` (or `Review Snapshot` for non-PR scope), copied exactly from `metadata.reviewer_assessments`: ordered `{role, rating, evidence}` records with unique readable participant labels, integer ratings 1–5 and retained assessment pointers. Snapshot `summary` copies `review_decision.summary` and remains aggregate prose above the table. The renderer appends `Reviewers | Role (rating), Role (rating).` and immediately below the table prints `Legend: 1 = Approve · 2 = Minor changes · 3 = Changes required · 4 = Insufficient evidence · 5 = Block / Reject.` Do not add this derived row to machine `rows`. Findings and operational blockers add ordered nonempty `authors` lists, matching reviewer labels and canonical record authors exactly. The renderer inserts `Author` after the ID column without changing existing machine cells or removing any summary, verdict, evidence, confidence or finding content. List all contributors to deduplicated findings. Never fabricate skipped reviewers or average ratings. Historical artifacts without attribution remain readable; terminal unassessed branches keep their no-table rules.

New code-remediate handoffs additionally require `commit_disposition` with exactly three nonempty string fields: `status`, `reason`, `evidence`. Status is `pending` (choice or authorized execution outstanding), `blocked` (verification/closure/ownership/destination/execution prevents committing), `not-applicable` (no remediation-owned tracked changes), `declined` (explicit matching leave-unstaged answer), or `committed` (all authorized units and post-commit checks finished). Reason explains the actual disposition, next owner/action for blocked work, and remaining unstaged work; evidence points to the existing plan, decision, gate or closure record. Pending and committed require every canonical gate passing or explicitly not applicable, `commit-plan.md`, and an existing evidence file declared as a run-relative POSIX path without symlink components, traversal, or fragments. A passing result requires selected closure except explicitly user-deferred work outside the plan, with consistent deferred counts, user-owned `user-deferred` reason groups, no unresolved local/process/environment/external-owner counts, and local actionable closure complete. A failed result may permit a local commit only after an explicit user request when its sole selected open work is environment-owned verification, all local actionable findings are closed, no canonical gate or critical finding failed, and `commit-plan.md` records `## Explicit Commit Request` and `## Remaining Verification`; result and unresolved item statuses stay failed/open. Selected outcome rows must agree with summary counts; only unresolved rows explicitly marked `Deferred: ...` qualify for the passing-result exception, while environment-only failed-result rows remain `Blocked: ...`. The workflow owner must verify user authorization, excluded plan paths, actual staged paths, and that the commit message discloses the missing verification under `Residual limits:`. Automated checks establish file existence and metadata consistency, not the truth of plan prose, user consent, or Git ownership. Record earlier authorization and created hashes in the plan; a pending disposition never authorizes staging or proves a question was delivered.

The renderer requires this record for every new remediation write and emits it once after verification; candidate validation rejects its omission. Historical `check` and canonical-result reads preserve older records without it. Other skills cannot add it. Explicit caller-contract output retains its exact bytes while the machine handoff still records the disposition. Validation proves structure and gate/result consistency, not the truth of consent or Git ownership; existing workflow preflight and actual control calls remain mandatory.

`final-handoff.json` has exactly these top-level fields: `schema_version`, `skill`, `branch`, `outcome`, `tables`, `source_records`, `source_coverage`, `verification`, `remaining`, `next_steps`, `confidence`, `artifacts`, `caller_contract`. Schema version is `1`. The helper's executable validation is authoritative for nested types, closed vocabularies, exact columns, row/source uniqueness, completeness counts, confidence bands, closure evidence, branch-specific table rules.

New workflow handoffs set `presentation_version=3`; historical v2 retains its exact rendered bytes and omitted version retains the v1 compatibility path. Both v2 and v3 start with plain-English outcome prose, omit an empty Results section, render one concise checks-not-run line when all gates are not applicable, and render each recovery action once with its owner. V3 also prefixes remaining next steps with their row IDs; v2 keeps its original unprefixed text. The complete machine `outcome`, `verification`, `remaining`, `next_steps` records stay unchanged. Exact caller-contract output stays exact.

Normal artifact workflows use `branch=standard`; `code-review` uses `assessed`, `unavailable`, or `closed`. An explicitly requested exact caller output uses `caller-contract`, records requested format and evidence, forbids normal tables, emits only `caller_contract.output`. Never infer this override merely because concise output would be convenient.

New review-findings and remediation tables use `layout=concise`. Exact machine columns, row identities, source coverage, detail bindings stay unchanged. Attributed review overview: `ID | Author | Finding | Resolution proposal | Status`; historical unattributed overview: `ID | Finding | Resolution proposal | Status`; required_change supplies a short concrete proposal. Remediation overview: `ID | Severity | Finding | Resolution | Outcome`; resolution expands the exact bound O symbol. Version-3 remediation handoffs require `overview_only=true` on the concise table so chat omits repeated ID detail blocks; an `action-items.md` artifact link is then required because that Markdown resolution ledger retains complete sources and evidence. Older concise handoffs without this field keep their saved rendering. Review ID-only detail groups add context, full evidence, closure criteria, or next actions without repeating finding titles, proposals/resolutions, or status. Finding names the problem; Context explains its mechanism, not a second Issue field. Review rows carry their canonical `title`, `summary`, `closure_evidence`; shared validator binds these and action/evidence cells to the enriched review record. Operational blockers carry canonical `id`, descriptive `title`, `required_change`, `evidence`, and omit summary/closure. Other skills, PR Snapshot and Review Snapshot don't support concise or grouped layout. Historical grouped/legacy rendering and ID-only blockers stay readable; never retroactively rewrite validated handoffs.

Before remediation scope selection, `final_handoff.py selection` validates `selection.json`, renders `resolution-scope.md`; `--check` verifies exact existing bytes without writes. The inventory contains `schema_version=1`, `presentation_version=3`, `selected_indexes=null` (pending) or a confirmed numeric list, and ordered items with `input_item_id`, `item_name`, `severity`, `selectable`, `summary`, `closure_evidence`, full sources. Selectable items also require `resolution_proposal`. The overview includes a short proposal and derived source-kind counts (`report ×1; online ×2`); full pointers appear once below. Repeated report views belong in source `related_mentions`, with `finding_id` when known — not independent sources. Duplicate item/source/canonical-finding identities or incorrect declared counts fail before any prompt. Record matching `resolution_scope.presentation_version=3`; final validation binds confirmed selection and source inventory to the outcome ledger. Version-2 selection rendering (including omitted inventory version) and legacy artifacts stay readable.

For version-3 remediation outcomes, `resolved_how` starts with a reasoned disposition using exact `<label>: <concrete reason>` syntax. The concise overview displays the reason as Resolution, the label as Outcome; internal status/counts and O/E bindings stay unchanged.

| Internal `resolution_status` | Allowed visible label |
| -- | -- |
| `implemented` | `Implemented` |
| `resolved`, `already-fixed`, `already-applied` | `Verified without code changes` |
| `rejected` | `Rejected` |
| `not-applicable` | `Not applicable` or `Rejected` |
| `duplicate` | `Duplicate` or `Rejected` |
| `needs-clarification` | `Needs clarification` |
| `unresolved` | `Blocked`, `Deferred`, or `Not selected` |

Deferral and nonselection require the actual user decision, not a substitute for investigating valid selected work. Never label blocked work rejected, or evidence-only closure implemented. Record finding-specific change evidence for implementation, actual closure evidence for verification without edits, concrete owner/action for open work. Zero implementations must be explicit in the opening; target integration and green CI are not review fixes. Passing gates stay separate from whether all selected remediation is complete. Historical grouped/legacy output retains its bytes.

`stale` is not a current remediation triage or resolution status. Outdated anchors and conflict-resolution line changes require reassessing the original concern against current code, never dismissal or automatic conversion to `rejected`. Other dispositions retain their existing evidence requirements. Legacy count keys remain for historical compatibility, must be zero in current results.

Selection items also retain nonempty `item_type`; report-origin `review-gate` and `confidence-gap` types determine gate intake counts. Report identity defaults to the full source file path; explicit shared `report_id` is required for verified cross-file aliases. Shared closure text never establishes finding equivalence. Input/output paths must be distinct; output must not be a symlink.

For normal table branches, every material finding, decision, changed surface, experiment, recommendation, or source-owned remediation item must have one stable row ID. `source_records` is the complete source ledger; each table row names its represented source IDs; `source_coverage.omitted_source_records_total` must be zero. A table may carry ordered `details` entries with exactly `id` and `text`; every detail ID must be unique, referenced as `[<id>]` by cell in that table, with the renderer placing definitions immediately below the table. Remaining work names the related row ID, item, owner, next action; `next_steps` contains exactly the remaining row IDs. Terminal `code-review` branches contain no tables or source records.

`verification` preserves all five gate records in canonical order, uses the corresponding gate `stdout` artifact path as evidence. `confidence.score`, gaps, gap closure status/evidence/rationale, limits must exactly reconcile with the result's confidence metadata. `artifacts` must include the candidate/final result path.

New `code-review` unavailable results occur before PR verification: every canonical PR gate must be `not-applicable` with a nonempty reason, and the versioned handoff mirrors those records as checks not run. A successful diagnostic command is not a passing PR check; retain recovery diagnostics separately. Generic gate execution retains normal exit-code semantics. Historical schema-v1 results and presentation-v1 rendered bytes keep their compatibility contract; new results can't use that path to bypass applicability validation.

Every pause follows [Actionable Pauses](native-skill-contract.md#actionable-pauses), including terminal review unavailability and assessed review with a withheld executable check. Use existing fields: `outcome.summary` states the stopped action, concrete cause/evidence, governing rule; the related `remaining` entry names continuing work in `item`, the responsible actor in `owner`, feasible recovery plus exact resume condition in `next_action`. Keep that entry in `next_steps`. For assessed code review, whose outcome is a fixed recommendation, place the complete explanation in the related remaining item and canonical operational blocker when applicable. An exact caller contract must preserve the same explanation within its allowed output. Never reduce handoff to an error code, generic approval request, or instruction to retry unchanged after deterministic failure.

Use the existing `next_action` text for recommended recovery, pending decision identity, consequences, resume condition. When a decision is needed, follow [User Questions](native-skill-contract.md#user-questions): show required context first, then let one native control own the live question and choices; only the plain-chat fallback includes them in `next_action`. Keep the exact original question, accepted answers, scope binding, actual response in existing durable workflow notes without extending closed schemas. Never duplicate a native question in rendered final Markdown. Distinguish continuing work from work paused until a specific condition; never imply a temporary process failure permanently blocks the goal. Keep canonical branch wording, schemas, evidence bindings, historical bytes unchanged. An unvalidated process pause follows the same guidance without claiming a validated final handoff.

For v2 unavailable review, bind `outcome.summary` to the actual `metadata.collection_failure.code` and listed safe diagnostic artifacts. It begins with a bounded plain-English stage explanation, then `Reason: <code>`, then only present safe command, checkout, or worktree-preflight diagnostics. When no safe collector reason exists, state once that a more specific cause was not retained; never make that statement when a safe reason is present. Known collection labels use the static stage map; unknown label uses `Source collection stopped before I could verify which PR revision to review.` `local-pr-checkout` says checkout did not complete; `checkout-paths` says local files could not be compared. Never infer checkout failure from missing evidence. `pr-error.txt` is always listed; `command-failure.json`, `checkout-state.json`, `worktree-preflight.json` are listed only when present and validated. A preflight may expose only validated current and expected head identifiers, never its path lists. The single `remaining`/`next_steps` record has an arbitrary stable row ID, `owner=code-review`, the exact failure code in `item`, nonempty recovery action containing the classified operation token plus exact resume condition `Resume only after a fresh collector run produces and validates the PR source bundle.` This permits richer evidence-backed recovery or protected-state decision while rejecting generic repair-only and unrelated CI actions. An unavailable run must not contain `local-checkout.json`.

## Exact table columns

Historical `change-analysis` artifacts retain their original skill identity, use the `assess` column contract. Report-reading compatibility only; the installed skill is named `assess`.

| Skill | Columns |
| -- | -- |
| `challenge-resolve` | `Challenge \| Security \| Critical \| High \| Medium \| Low \| Nits \| Weighted score \| Decision \| Evidence` |
| `audit` | `Item \| Severity / impact \| Decision \| Evidence \| Next action` |
| `calibrate` | `Check / metric \| Result \| Evidence \| Next action` |
| `assess` | `Finding \| Impact \| Decision \| Evidence \| Next action` |
| `code-remediate` | `Item \| Severity \| Finding \| Sources \| Outcome \| Evidence / next action` |
| `implement` | `Surface \| Outcome \| Verification \| Remaining limit` |
| `investigate` | `Hypothesis \| Evidence \| Disposition \| Next action` |
| `kaggle` | `Artifact \| Mode \| Verification \| Runtime limit` |
| `manage` | `Surface \| Outcome \| Verification \| Remaining limit` |
| `optimize` | `Iteration \| Baseline \| After \| Delta \| Guard \| Decision` |
| `release` | `Change \| SemVer impact \| Status / blocker \| Evidence` |
| `research` | `Recommendation \| Evidence \| Decision \| Caveat / next check` |
| `sync` | `Surface \| Outcome \| Verification \| Remaining limit` |

Current `challenge-resolve` results set `metadata.challenge_table_contract_version=1`, use schema-2 loop evidence with one checked parent triage entry per raw reviewer finding, and declare `metadata.challenge_scope` as single or chunked under both candidate and canonical filenames; the chunked declaration binds literal `chunks/chunks.json` and `chunks/results.json` artifacts for a fresh coverage check. Each validated challenge row lists six severity cells and the weighted-score cell as literal `old + new`, including zero cells. Old means a currently open signature seen in any earlier completed round, even if it closed and reopened; new means a currently open signature first seen in this round. `open` and `fixed-pending-verification` count. State the legend beside the table. The shared validator binds every split, decision, and report path to the loop ledger. When no round is validated, use `not-run` with `N/A` for all severity and score cells. Historical JSON with earlier table formats remains readable as archive data but cannot validate current completion under either filename.

> Challenger review starts in a fresh isolated process with frozen source and review criteria. Resolver reports stay with the parent. Candidate and canonical results must pass the same current challenge artifact checks; a filename cannot select archived completion rules.

Current candidate and canonical chunked coordinator validation requires a schema-1 manifest; A current chunked coordinator with a schema-1 chunk manifest copies its declared first-run or continuation origin into `metadata.challenge_origin` under both result filenames; a declared continuation requires prior ledger and coverage. It uses `metadata.chunked_review` equal to the current validated `chunk_diff.py check --summary` JSON for complete clean coverage or `check-stopped --summary` for partial coverage. Copy its `runs` array into `final-handoff.json` as `child_runs`; the validator binds exact order, result paths, score series, decisions, and stopped reasons to the live checker output. Complete clean coverage permits passing result and review gate with zero findings; stopped coverage requires failed result and review gate plus a remaining item and next step. The renderer lists every child and interaction path with its own score series beneath Results, without an aggregate score or coordinator reviewer receipt. Historical version-2 handoffs retain their saved rendering and do not require `child_runs`. In both states, the Results row is `not-run | N/A` in every numeric cell, decision `chunk-only`, evidence `chunks/results.json`; the coordinator has no own loop ledger or `metadata.adversarial_loop`. Caller completion and undeclared interaction judgment remain separate gates.

New `release` communication-contract handoffs include a second table named `Readiness`, with `Check | Status | Evidence | Blocker / next action`. Its rows exactly match the `## Checks` table in `release-readiness.md`; each check has a distinct source record. The shared release artifact validator binds these rows. Historical one-table release handoffs retain their exact rendering.

An assessed `code-review` may use `PR Snapshot` with `Field | Value` and `Review Findings and Merge Blocks` with `Finding / area | Required change | Evidence | Status`. PR scope requires the snapshot; nonzero findings require the findings table. `code-remediate` rows are value-bound to `metadata.final_resolution_table.items`: cells are exactly `input_item_id`, `severity`, `item_name`, ordered compact `report [<report-file>:<line>]`, `report [<report-json>#<finding-id>]`, or `online [<comment|thread|review-id>]` or `online [<parent-id>#finding-<ordinal>]` pointers, `resolution_status — [O<n>]`, and `[E<n>] — owner/status: owner_status`. Ordered table details bind each `O<n>` to `resolved_how`, each `E<n>` to `evidence`; row IDs, `kind:source_id` values, source evidence, full source records in metadata and expanded ledger items must also match exactly.

## Result compatibility

`write-result.py` creates schema-2 results for other skills and schema-3 results for every current Code Review disposition. It requires `metadata.final_handoff` with schema version, three sibling paths, two SHA-256 digests, and branch. `validate-artifacts.py` checks those paths, exact rendered bytes, digests, gates, confidence, result artifact reference, and workflow-specific reconciliation. Historical results with no `schema_version` are read as schema 1 and remain valid without final-handoff artifacts; new results cannot opt into that compatibility path because the writer emits schema 2 or 3.

## Transport limit and manager exception

The checkpoint proves the complete response bytes the workflow must emit, but the current host has no post-send transcript hook that can prove chat transport reproduced them. Treat any later manual rewrite as a contract violation; a future host transcript hook may close this residual transport gap without changing artifact schema.

`agent-shims` remains a documented lifecycle exception because it has no canonical `.reports` result artifact, is excluded from the artifact/calibration roster. Its final chat contract remains advisory until that manager gains a canonical run/result lifecycle; never claim executable final-response validation for it.
